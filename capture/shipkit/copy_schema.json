{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "GENERATED by scripts/gen_shipkit.py from spec/shipkit.v1.json — DO NOT EDIT. Handed to `claude -p --json-schema` by capture/shipkit/copy.py.",
  "title": "ShareCopy",
  "description": "The posts a builder could put beside a demo of what they built, one per platform. Every sentence describes work that is in the input. A number the input does not contain, written the same way, gets the whole caption thrown away; so does a claim the input does not make. Plain words, first person, no dashes.",
  "type": "object",
  "properties": {
    "captions": {
      "description": "One per platform: x, linkedin, threads, instagram, tiktok, bluesky. (1 to 6 items)",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Caption"
      },
      "maxItems": 6,
      "minItems": 1
    }
  },
  "required": [
    "captions"
  ],
  "additionalProperties": false,
  "$defs": {
    "Caption": {
      "type": "object",
      "properties": {
        "platform": {
          "description": "Which platform this post is written for.",
          "type": "string",
          "enum": [
            "x",
            "linkedin",
            "threads",
            "instagram",
            "tiktok",
            "bluesky"
          ]
        },
        "text": {
          "description": "The post, as it will be pasted. Within the platform's own limit (platform_limits). Every number in it must be one of the numbers in the input, written the same way; no number the input does not have, no claim the input does not make, no hashtag soup (at most three, only where the platform uses them), no dash of any kind. (max 3000 chars)",
          "type": "string",
          "maxLength": 3000
        },
        "thread": {
          "description": "X only: the same story as a thread of 2 to 5 posts, each within 280 characters. Empty for every other platform. (each max 280 chars, max 5 items)",
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 280
          },
          "maxItems": 5
        }
      },
      "required": [
        "platform",
        "text",
        "thread"
      ],
      "additionalProperties": false
    }
  }
}
