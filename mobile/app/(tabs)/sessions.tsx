/**
 * The Sessions tab: this week on a band in your hue, then every session as an open row with its
 * creature and its strip. The screen and its rules live in `src/session/SessionsScreen.tsx`.
 */
import { SessionsScreen } from '../../src/session/SessionsScreen';

export default SessionsScreen;
