/**
 * `builder://analysis`: every piece of analysis on one page. Pushed over the tabs; the page and
 * its rules live in `src/insights/`.
 */
import { AnalysisScreen } from '../src/insights/AnalysisScreen';

export default AnalysisScreen;
