/**
 * `builder://project/<key>`: one project's page, pushed over the tabs. `key` is the repository's
 * 64 hex key, or the 12 character prefix the profile lists. The page and its rules live in
 * `src/projects/` (`ProjectPage.tsx`, `model.ts projectPage`).
 */
import { ProjectPage } from '../../src/projects/ProjectPage';

export default ProjectPage;
