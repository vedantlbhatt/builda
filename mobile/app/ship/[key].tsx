/**
 * `builder://ship/<key>`: a project's ship kit and one Share (docs/ship-kit.md). The screen and its
 * rules live in `src/shipkit/`.
 */
import { ShipKitScreen } from '../../src/shipkit/ShipKitScreen';

export default ShipKitScreen;
