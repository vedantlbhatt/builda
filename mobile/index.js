// The app's entry on iOS and Android: exactly expo-router's own. `package.json` names `index`
// so that web can have `index.web.js` beside it; native bundles never read that file.
import 'expo-router/entry';
