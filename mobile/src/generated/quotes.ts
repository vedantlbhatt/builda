// GENERATED by scripts/gen_contract.py from privacy/upload-contract.json — DO NOT EDIT.

/** The quotes document: PUT /v1/profile/quotes, default off. Owner only, opt in twice. */
export const QUOTES_ROUTE = "PUT /v1/profile/quotes";

/** Character caps by size class; string fields below name one of these in their doc. */
export const QUOTES_MAX_LENGTHS = {
  quote: 160,
} as const;

export type QuoteCard = "go_to_prompt" | "crash_out" | "cryptic_prompt";

export interface QuoteWire {
  card: QuoteCard;
  /** wrapped._quote: whitespace collapsed, cut at the last space inside 160 (max 160 chars) */
  text: string;
  /** the session it was sent in, already on the server (64 lowercase hex chars) */
  client_session_id: string;
  /** (ISO 8601) */
  sent_at: string;
  /** from that session's start to this prompt (the crash out sentence) */
  seconds_in: number;
  /** cryptic_prompt only */
  tool_calls_after?: number | null;
  /** cryptic_prompt only */
  corrected?: boolean | null;
}

export interface QuotesUpload {
  quotes_version: number;
  /** (ISO 8601) */
  generated_at: string;
  /** (max 3 items) */
  quotes: QuoteWire[];
}
