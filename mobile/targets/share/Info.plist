<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDisplayName</key>
  <string>Builda</string>
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.share-services</string>
    <key>NSExtensionPrincipalClass</key>
    <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
    <key>NSExtensionAttributes</key>
    <dict>
      <!--
        A PREDICATE, not the simple activation dictionary. The simple form
        (NSExtensionActivationSupportsWebURLWithMaxCount) also matches an attachment with no URL
        in it at all on some hosts, and Instagram's share item carries BOTH a URL and a plain
        text copy of it, which the simple form counts as two items and refuses at MaxCount 1.
        This says what it means: at least one URL or one string, and nothing about how many.
      -->
      <key>NSExtensionActivationRule</key>
      <string>SUBQUERY(extensionItems, $item, SUBQUERY($item.attachments, $att, ANY $att.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url" || ANY $att.registeredTypeIdentifiers UTI-CONFORMS-TO "public.plain-text").@count &gt; 0).@count &gt; 0</string>
    </dict>
  </dict>
</dict>
</plist>
